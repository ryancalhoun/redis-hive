---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-master
spec:
  selector:
    app: {{ .Release.Name }}
  ports:
    - protocol: TCP
      port: {{ .Values.redis.port }}
      targetPort: {{ .Values.hive.proxy.port }}
